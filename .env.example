# Required for stages 04+ (LLM summarization, embeddings, topic naming, Typologist)
CO_API_KEY=             # Cohere API key (embeddings)
ANTHROPIC_API_KEY=      # Anthropic API key (summarization, topic naming, Typologist)

# No external auth needed for stages 00-03 (FronkonGames is public, Steam APIs are unauthenticated).
